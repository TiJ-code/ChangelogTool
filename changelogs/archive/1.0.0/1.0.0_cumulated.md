## Archive
- added phase-aware archive directories for versioned changelogs
- added direct version archive directories for single-phase configurations
- refactored archive processing to consume the versioning configuration, shared versioning model, and configured formatter

## Config Parser
- added stable configuration format version 1 with XSD validation
- added configuration-driven version sources, development phases, phase formatters, snapshot state, and phase transition events
- added initial and release phase roles in the stable version 1 configuration format
- refactored obsolete prerelease configuration parser versions and compatibility branches
- refactored configuration parsing to support configurable versioning phases, formatters, and optional phase-entry events

## Markdown
- refactored topic-aware cumulative changelog parsing and Markdown rendering

## Patches
- added stable patch format version 1 with XSD validation and topic-scoped component changes
- refactored obsolete prerelease patch parser versions and DTD-based validation

## Versioning
- added configured numeric version increments and phase transitions
- added independent snapshot state for unreleased versions within a development phase
- added version source consistency validation across configured files
- added version change-set planning before applying coordinated file modifications
- added shared versioning model and subsystem tests for formatters, sources, consistency, and change application
- refactored versioning CLI commands to operate through generic configured operations
- refactored numeric increments to enter the configured initial phase as snapshots
- refactored release operations to clear snapshot state without changing the numeric version
- refactored next-phase operations to follow configured transitions and apply phase-entry events

## XML Model
- added topic identity to the shared XML component model


